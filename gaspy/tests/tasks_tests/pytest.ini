[pytest]
addopts = -m "not baseline"
filterwarnings =
    ignore::DeprecationWarning
markers =
    baseline: used only to create baselines for regression unit testing (select with '-m "baseline"')
    serial
